[package]
name = "anywhere-computer-manager"
version = "0.1.0-alpha.10"
edition = "2021"
license = "MIT"
publish = false

[build-dependencies]
tauri-build = { version = "=2.6.3", features = [] }

[dependencies]
tauri = { version = "=2.11.5", features = [] }
tokio = { version = "1", features = ["process", "io-util", "time"] }
serde_json = "1"
